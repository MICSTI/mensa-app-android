<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="itm.fhj.at.mensaapp" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LocationsList"
            android:label="@string/title_activity_locations_list" >
        </activity>
        <activity
            android:name=".activity.MensaDetail"
            android:label="@string/title_activity_mensa_detail" >
        </activity>
        <activity
            android:name=".activity.FavouriteMealsList"
            android:label="@string/title_activity_favourite_meals_list" >
        </activity>

        <service
            android:name=".service.FavouriteMealService"
            android:enabled="true"
            android:exported="true" >
        </service>
    </application>

</manifest>
